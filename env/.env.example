# MVP Tile Server - Environment Configuration
# Copy this file to .env and customize values

# Port to expose the tile server on the host
TILE_SERVER_PORT=8000

# Path to tile data directory on the host
# This directory is mounted to /app/data in the container
TILE_DATA_PATH=./test_data

# Logging level (debug, info, warning, error)
LOG_LEVEL=info
